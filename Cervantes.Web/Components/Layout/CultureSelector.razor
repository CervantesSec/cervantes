@using System.Globalization
@using Cervantes.Web.Localization
@inject IStringLocalizer<Resource> localizer
@inject NavigationManager NavigationManager


<MudMenu FullWidth="true" Variant="Variant.Filled" AnchorOrigin="Origin.BottomCenter" TransformOrigin="Origin.TopCenter">
                            
    <ActivatorContent>
        <MudIconButton Icon="@Icons.Material.Filled.Language" IconColor="Color.Tertiary" Size="Size.Medium"></MudIconButton>

        @*
        @switch (Culture.Name)
        {
            case "en-US":
                <MudStack Row="true" AlignItems="AlignItems.Center">
                    <MudIcon Icon="@Icons.Material.Filled.Language" IconColor="Color.Tertiary" Size="Size.Medium"></MudIcon>
                    <MudText Typo="Typo.body2">@localizer["english"]</MudText>
                </MudStack>
                break;
            case "tr-TR":
                <MudStack Row="true" AlignItems="AlignItems.Center">
                    <MudIcon Icon="@Icons.Material.Filled.Language" IconColor="Color.Tertiary" Size="Size.Medium"></MudIcon>
                    <MudText Typo="Typo.body2">@localizer["turkish"]</MudText>
                </MudStack>
                break;
            case "es-ES":
                <MudStack Row="true" AlignItems="AlignItems.Center">
                    <MudIcon Icon="@Icons.Material.Filled.Language" IconColor="Color.Tertiary" Size="Size.Medium"></MudIcon>
                    <MudText Typo="Typo.body2">@localizer["spanish"]</MudText>
                </MudStack>
                break;
            case "pt-PT":
                <MudStack Row="true" AlignItems="AlignItems.Center">
                    <MudIcon Icon="@Icons.Material.Filled.Language" IconColor="Color.Tertiary" Size="Size.Medium"></MudIcon>
                    <MudText Typo="Typo.body2">@localizer["portuguese"]</MudText>
                </MudStack>
                break;
        }
        *@

    </ActivatorContent>
    <ChildContent>
        <MudMenuItem @onclick="@((e) => CultureSet(supportedCultures[0]))">@localizer["english"]</MudMenuItem>
        <MudMenuItem @onclick="@((e) => CultureSet(supportedCultures[1]))">@localizer["spanish"]</MudMenuItem>
        <MudMenuItem @onclick="@((e) => CultureSet(supportedCultures[2]))">@localizer["portuguese"]</MudMenuItem>
        <MudMenuItem @onclick="@((e) => CultureSet(supportedCultures[3]))">@localizer["turkish"]</MudMenuItem>
    </ChildContent>
</MudMenu>

